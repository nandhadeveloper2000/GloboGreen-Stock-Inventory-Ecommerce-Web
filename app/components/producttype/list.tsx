"use client";

import ProductTypePlaceholder from "./shared";

export default function ProductTypeListPage() {
  return (
    <ProductTypePlaceholder
      title="Product Type List"
      description="This legacy route has been kept stable so it no longer crashes the app, even though the product type listing feature is not available in the current build."
    />
  );
}
