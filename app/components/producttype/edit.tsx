"use client";

import ProductTypePlaceholder from "./shared";

export default function EditProductTypePage() {
  return (
    <ProductTypePlaceholder
      title="Edit Product Type"
      description="The edit route now loads safely, but there is no dedicated product type module wired into the current application."
    />
  );
}
